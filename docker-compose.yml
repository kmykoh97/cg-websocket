version: '3'

services:
  web:
    build: .
    command: bash -c "python manage.py runserver 0.0.0.0:8000"
    container_name: cg_websocket
    volumes:
      - .:/code
    ports:
      - "80:8000"
    depends_on:
      - redis
    restart: always

  redis:
    image: "redis:latest"